apiVersion: coligo.cloud.ibm.com/v1alpha1
kind: JobRun
metadata:
  generateName: classify-images-
spec:
  arraySize: 1
  retryLimit: 2
  jobDefinitionSpec:
    containers:
    - name: coligo-jobs
      image: ibmcom/backend-job
      envFrom:
      - secretRef:
         name: cos-secret
      - secretRef:
         name: vr-secret
      resources:
        requests:
          memory: "128Mi"
          cpu: "1"